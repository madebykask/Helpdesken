import { Routes, UrlSegment } from '@angular/router';
import { ExtendedCaseComponent } from './app/components/extended-case.component';

export const routes: Routes = [
    { path: '', component: ExtendedCaseComponent},
];


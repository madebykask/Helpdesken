import { Routes, UrlSegment } from '@angular/router';
import { ExtendedCaseRouter } from './app/router.component';

export const routes: Routes = [
    { path: '', component: ExtendedCaseRouter},
];


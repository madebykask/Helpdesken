@using DH.Helpdesk.Web.Infrastructure
@model DH.Helpdesk.Web.Areas.Inventory.Models.EditModel.ComputerModuleEditModel
@{
    Layout = "~/Areas/Inventory/Views/Shared/_EditLayout.cshtml";
}

@section Actions
{
    <ul class="secnav">
        @if (Model.UserHasInventoryAdminPermission)
        {
            <li id="menu1">
                <input type="submit" value="@Translation.Get("Spara")" class="btn" onclick=" $('#computerModule_edit_form').submit(); "/>
            </li>
            <li id="menu3">@Html.ActionLink(Translation.Get("Ta bort"), "Delete", new { id = Model.Id }, new { @class = "btn" })</li>
        }
        <li id="menu2">@Html.ActionLink(Translation.Get("Avbryt"), "Index", null, new { @class = "btn" })</li>
    </ul>
}

@section Title
{
    @Translation.Get("Ram")
}

@section Content
{
    <h4>@Translation.Get("Allmänt")</h4>
    <div class="row">
    <div class="span3">
        @Translation.Get("Ram")
    </div>
    <div class="span9">
    @using (Html.BeginForm("Edit", "Ram", FormMethod.Post, new { id = "computerModule_edit_form" }))
    {
        @Html.EditorFor(m => m.Name)
        @Html.ValidationMessageFor(m => m.Name)
        @Html.HiddenFor(m => m.Id)
    }
    </div>
    </div>
}
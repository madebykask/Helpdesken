@using DH.Helpdesk.Web.Infrastructure
@model DH.Helpdesk.Web.Areas.Inventory.Models.SearchModels.ReportsIndexViewModel
@{
    Layout = "~/Areas/Inventory/Views/Shared/_IndexLayout.cshtml";
}

@section Scripts
{
    <script>
        $(function () {
            $('#type_dropdown').change(function () {
                $('#type_form').submit();
            });
        });
    </script>
}

@section Actions
{
    @RenderSection("Actions")
}

<table class="tableform nobm">
    <tr>
        <td class="span3">@Translation.Get("Rapport")</td>
        <td>
            @using (Html.BeginForm("RedirectToReportIndex", "InventoryRouting", FormMethod.Post, new { id = "type_form" }))
            {
                @Html.DropDownListFor(x => x.ReportType, Model.ReportTypes, new { id = "type_dropdown" })
            }
        </td>
    </tr>
</table>

@RenderBody()
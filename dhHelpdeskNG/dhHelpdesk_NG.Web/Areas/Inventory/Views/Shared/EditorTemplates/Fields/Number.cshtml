@using DH.Helpdesk.Web.Infrastructure.Extensions.HtmlHelperExtensions
@using DH.Helpdesk.Web.Infrastructure
@model DH.Helpdesk.Web.Areas.Inventory.Models.EditModel.ConfigurableFieldModel<int>
@if (!Model.Show)
{
    return;
}

@{
    var validationMessage = string.Format(" {0} {1}", Html.PureFieldCaption(Model), Translation.Get("måste anges"));
}
<tr>
    <td class="span3">
        @Html.FieldCaption(Model)
    </td>
    <td class="span9">
        @if (!Model.IsReadOnly)
        {
            @Html.TextBoxFor(m => m.Value, new Dictionary<string, object> { { "data-dh-is-required", Model.IsRequired } })
            @Html.HiddenFor(m => m.IsRequired)
            @Html.ValidationMessageFor(m => m.Value, validationMessage)
        }
        else
        {
            @Html.TextBoxFor(m => m.Value, new { @readonly = true })
        }
    </td>
</tr>

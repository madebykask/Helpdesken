@using DH.Helpdesk.Web.Infrastructure
@model DH.Helpdesk.Web.Areas.Admin.Models.GlobalSettingIndexViewModel
<div class="row">
    <div class="span8">
        <table class="table table-striped table-bordered table-hover" id="tblTextTranslation">
            <thead>
                <tr>
                    <th>
                        @Translation.Get("Text", Enums.TranslationSource.TextTranslation)
                    </th>
                    <th>
                        @Translation.Get("Skapad datum", Enums.TranslationSource.TextTranslation)
                    </th>
                    <th>
                        @Translation.Get("Senast ändrad datum", Enums.TranslationSource.TextTranslation)
                    </th>
                    <th>
                        @Translation.Get("Skapad av", Enums.TranslationSource.TextTranslation)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.TextWithUsers)
                {                    
                    <tr>
                        <td style="width: 200px">
                            <a href="@Url.Action("edittranslation", "globalsetting", new { area = "admin", id = item.Id })">@Html.DisplayFor(modelItem => item.TextToTranslate)</a>
                        </td>
                        <td>
                            @item.CreatedDate.ToShortDateString()
                        </td>
                        <td>
                            @item.ChangedDate.ToShortDateString()
                        </td>
                        <td>
                            @item.FirstName  @item.LastName
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

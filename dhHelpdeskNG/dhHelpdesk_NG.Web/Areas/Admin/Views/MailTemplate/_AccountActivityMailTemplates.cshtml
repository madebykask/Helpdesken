@using DH.Helpdesk.Web.Infrastructure
@model DH.Helpdesk.Web.Areas.Admin.Models.MailTemplateIndexViewModel
<table class="table table-striped table-bordered table-hover">
    @foreach (var item in Model.AccountActivities)
    {
        <tr><td>@Translation.Get("E-post mall: Beställning kontoaktivitet- ", Enums.TranslationSource.TextTranslation)<a href="@Url.Action("edit", "mailtemplate", new { area = "admin", id = item.Id, customerId = Model.Customer.Id })">@Html.DisplayFor(modelItem => item.Name)</a>
        </td></tr>
    }
</table>

@using DH.Helpdesk.Web.Infrastructure
@using DH.Helpdesk.Web.Infrastructure.Extensions
@model IList<DH.Helpdesk.Domain.Room>
@{
    ViewBag.Title = "";
}
<div class="secnavs fixed">
    <div class="pull-left">
        <ul class="secnav">
            <li>@Html.ActionLink(Translation.Get("Lägg till", Enums.TranslationSource.TextTranslation) + " " + Translation.Get("rum", Enums.TranslationSource.TextTranslation), "new", null, new { @class = "btn" })</li>
        </ul>
    </div>
    @Html.Partial("_MasterDataPanel")
</div>
<div class="content">
    <div class="container">
        <ul class="nav nav-tabs" id="myTab">
            <li class="active"><a href="#fragment-1">@Translation.Get("Rum", Enums.TranslationSource.TextTranslation)</a></li>
        </ul>
        <div class="tab-content">
            <div class="tab-pane tab-pane-border active" id="fragment-1">
                <table class="table table-striped table-bordered table-hover" id="tblroom">
                    <thead>
                        <tr>
                            <th>
                                @Translation.Get("Byggnad", Enums.TranslationSource.TextTranslation)
                            </th>
                            <th>
                                @Translation.Get("Våning", Enums.TranslationSource.TextTranslation)
                            </th>
                            <th>
                                @Translation.Get("Rum", Enums.TranslationSource.TextTranslation)
                            </th>
                            <th>
                                @Translation.Get("Aktiv", Enums.TranslationSource.TextTranslation)
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <a href="@Url.Action("edit", "room", new { area = "admin", id = item.Id })">@Html.DisplayFor(modelItem => item.Floor.Building.Name)</a>
                                </td>
                                <td>
                                    <a href="@Url.Action("edit", "room", new { area = "admin", id = item.Id })">@Html.DisplayFor(modelItem => item.Floor.Name)</a>
                                </td>
                                <td>
                                    <a href="@Url.Action("edit", "room", new { area = "admin", id = item.Id })">@Html.DisplayFor(modelItem => item.Name)</a>
                                </td>
                                <td>
                                    <a href="@Url.Action("edit", "room", new { area = "admin", id = item.Id })">@Translation.Get(item.IsActive.TranslateBit(), Enums.TranslationSource.TextTranslation)</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

 
@model DH.Helpdesk.Web.Models.Notifiers.InputModel
           
<div class="container">
    <h4>@Translation.GetCoreTextTranslation("Status")</h4>
    <div class="row">
        <div class="span12">
            <table class="tableform">
                <tr>
                    <td class="span3">
                       @Translation.GetCoreTextTranslation("Aktiv")
                    </td>
                    <td class="span9">
                        @Html.CheckBoxFor(m => m.IsActive, Model.IsReadOnly ? (object)new { @class = "switchcheckbox", @readonly = "readonly" } : new { @class = "switchcheckbox" })
                    </td>
                </tr>
                @if (!Model.IsNew)
                {
                    @Html.DisplayFor(m => m.CreatedDate, "Label")
                    @Html.DisplayFor(m => m.ChangedDate, "Label")
                    @Html.DisplayFor(m => m.SynchronizationDate, "Label")
                }
            </table>
        </div>
    </div>
</div>
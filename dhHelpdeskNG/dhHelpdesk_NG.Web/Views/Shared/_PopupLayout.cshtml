@using System.Web.Optimization
<!DOCTYPE html>
<html>
    <head>
        <meta content="html/text; charset=utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1" />
        <title>@Translation.GetCoreTextTranslation("dhHelpdesk")</title>
        <script type="text/javascript">
            var trans_yes = '@Translation.GetForJS("Ja")';
            var trans_no = '@Translation.GetForJS("Nej")';
            var placeholder_text_multiple = '@Html.Raw(Translation.GetCoreTextTranslation("Välj en eller flera"))';
            var placeholder_text_single = '@Html.Raw(Translation.GetForJS("Välj ett av alternativen"))';
            var no_results_text = '@Translation.GetForJS("Inga resultat matchar")';
        </script>
        @Styles.Render("~/Content/css/popup")
        @Styles.Render("~/img-profile/css")
        @Scripts.Render("~/bundles/common/popup")
        @RenderSection("Styles", false)
    </head>
    <body>
        <div class="popup">
            @RenderBody()
        </div>
    </body>
    @Scripts.Render("~/bundles/dhhelpdesk")
    @RenderSection("Scripts", false)
</html>
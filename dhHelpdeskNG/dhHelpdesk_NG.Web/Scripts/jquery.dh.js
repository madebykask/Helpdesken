

/*!
*   Datahalland jquery plugins
*/

; (function ($) {

    $.fn.tabify = function (options) {

        var settings = {
            
        };

        return this.each(function () {

            if (options) {
                $.extend(settings, options);
            }

            // tabify plugin code here

        });

    };
})(jQuery);

